# Global module
Helper-functions/options that modules are allowed to rely on.
Part of the 'default' module, which every configuration _must_ add.
Some files also just use 'imports' direcly, which are always automatically added.
